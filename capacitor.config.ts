import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.insightfulhabits.app',
  appName: 'InsightfulHabits',
  webDir: 'dist'
};

export default config;
